---
import Layout from "../layouts/Layout.astro";
import Card from "../components/Card.astro";
import NavigationBar from "../components/NavigationBar.astro";
import Footerdiv from "../components/Footer.astro";
---

<!DOCTYPE html>
<html lang="zh">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>欢迎来到Astro</title>
    <style>
        /* 页面基础样式 */
        body, html {
            height: 100%;
            margin: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(to right, #FFC0CB, white); /* 粉色到白色的渐变 */
        }

        /* 主要内容区域样式 */
        main {
            margin: auto;
            padding: 1rem;
            width: 800px;
            max-width: calc(100% - 2rem);
            color: pink;
            font-size: 20px;
            line-height: 1.6;
        }

        /* 导航链接样式 */
        nav a {
            color: purple;
        }

        /* 标题样式 */
        h1 {
            font-size: 4rem;
            font-weight: 700;
            line-height: 1;
            text-align: center;
            margin-bottom: 1em;
        }

        h2 {
            text-align: center;
        }

        /* 文本渐变样式 */
        .text-gradient {
            background-image: linear-gradient(to right, #FFC0CB, white);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-size: 400%;
            background-position: 0%;
        }

        /* 指令样式 */
        .instructions {
            margin-bottom: 2rem;
            border: 1px solid rgba(255, 192, 206, 0.25);
            background: linear-gradient(
                rgba(255, 135, 135, 0.66),
                rgba(255, 135, 135, 0.33)
            );
            padding: 1.5rem;
            border-radius: 8px;
            color: #FFC0CB; /* 确保文字颜色与背景协调 */
        }

        .instructions code {
            font-size: 0.8em;
            font-weight: bold;
            background: rgba(255, 192, 206, 0.12);
            color: #FFC0CB;
            border-radius: 4px;
            padding: 0.3em 0.4em;
        }

        .instructions strong {
            color: #FFC0CB;
        }

        /* 链接卡片网格样式 */
        .link-card-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(24ch, 1fr));
            gap: 2rem;
            padding: 0;
        }

        /* 页脚样式 */
        .footer {
            text-align: center;
            padding: 20px 0;
            color: #333;
        }

        /* 响应式设计样式 */
        @media (max-width: 600px) {
            h1 {
                font-size: 3rem;
            }

            /* 可能需要的其他响应式调整 */
        }
    </style>
</head>
<body>
    <Layout title="欢迎来到Astro">
        <main>
            <!-- 导航栏组件 -->
            <NavigationBar />
            
            <h1>考研笔记</h1>
            <h2>P211012582<span class="text-gradient">马丽</span></h2>
            <h2>考研笔记</h2>
            <p>在这个页面，我将展示有关我的25考研笔记整理笔记</p>
            <ul class="link-card-grid">
                <li><a href="/md/3/">25马理论考研资料整理</a></li>
            </ul>
        </main>
        <!-- 页脚组件 -->
        <Footerdiv />
    </Layout>
</body>
</html>